# AGENTS.md instructions for /home/marcelo/Code/financial-time-series-forecasting

Sempre consulte e aplique `docs/ai/AGENT_CORE.md` antes de executar qualquer
tarefa neste repositorio.

Tambem consulte `docs/ai/INDEX.md` no inicio de cada tarefa para descobrir o
catalogo local de skills em `docs/ai/skills/*`.

Para cada tarefa:
- selecione e carregue apenas as skills locais aplicaveis;
- declare por passo quais skills foram usadas, conforme a politica de
  rastreabilidade de `AGENT_CORE`;
- se nenhuma skill especifica for aplicavel, declare explicitamente
  `none specific (AGENT_CORE only)`.
